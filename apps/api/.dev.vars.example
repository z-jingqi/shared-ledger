# Local-only secrets. Copy to .dev.vars; do not commit the copy.
# AI_ORCHESTRATOR and ALEPH_TOOLS are Wrangler service bindings, not URL vars.
ALEPH_AI_SERVICE_TOKEN=
ALEPH_TOOLS_API_KEY=dev-key
ALEPH_TOOLS_WEBHOOK_SECRET=aleph-tools-local-webhook-secret
